
import { createRotatedPDF } from './pdfRotationOperations';
import { getRotatedTransform } from './pdfTransformUtils';

// Re-export the functions from the separated files for backward compatibility
export { createRotatedPDF, getRotatedTransform };
